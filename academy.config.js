module.exports = (params) => {

  return {
    webpack: (config) => {
      return config;
    },

    babel: (config) => {
      return config;
    },

    postcss: (config) => {
      return config;
    }
  }
};